# Test environment — points at a SEPARATE, disposable database.
# Never run the suite against dev/prod data (SPEC §5).
DATABASE_URL="postgresql://teamwiki:teamwiki@localhost:5433/teamwiki_test?schema=public"
JWT_SECRET="test-secret-not-for-production"
ACCESS_TOKEN_TTL="15m"
IMPORT_ROOT="./import-docs"
